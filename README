This plugin is for taxing all players (online and offline) on a server.  TaxThem will currently only work with Essentials as it needs to access the Essentials/userdata directory to get all of the players who have logged onto a server.